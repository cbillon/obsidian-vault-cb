  

[Video ](https://www.youtube.com/watch?v=JjcW0S60Ljo)
## Transcription

Hi everybody. Welcome to Moodle Composed. I'm  Andrew Lyons, the Principal Architect for Moodle  LMS. Let's discuss the problem that we're trying  to solve with this initiative. Well, Moodle uses  

### Chapitre 2 : The problem

many PHP packages and many of those have child  dependencies. Most modern PHP dependencies expect  you to use Composer and they have lots and lots  of dependencies and that's making it increasingly  difficult to not use Composer. Sometimes multiple  dependencies or multiple packages have shared  child dependencies and as a result you need to  carefully manage which ones may require which  different versions and solve any conflicts. At the  moment we have to do that manually. In addition,  some dependencies are opinionated. For example,  open telemetry requires a custom PHP extension be  installed on the server and that cannot easily  be done without using the composer ecosystem.  The open telemetry packages make use of the  composer API for discovery of other plugins  in within their ecosystem. On top of that, many  people in our community at Moodle want to be  able to manage their Moodle installation using  Composer. Now, in Moodle 5.1, we did introduce a new directory structure and we also made Composer  a requirement. Initially, we're not doing very  much with it, but that was in preparation for  supporting dependency installation. However,  that doesn't solve the issues with opinionated  dependencies or with Moodle plug-in installation.  

### Chapitre 3 : The solution

So, I've been looking at some of the ways that we  may want to address these challenges. And we're looking at introducing three new packages to the  composer and the packages ecosystems. The first is the **Moodle composer installer**. and that's a  composer plugin for placing dependencies in the correct location. The second is the **Moodle seed**  which is a composer sample project for seeding a new Moodle site. And finally, we have the  **Moodle composer scaffold** which provides tooling to simplify running Moodle using composer. Keep  in mind that this is an optional approach. You can use it but you don't have to use it. you  can coexist it uh with other sites on using the current method and it is retrofitable if you  decide to choose to adopt it on an existing Moodle site in the future. So onto a demo. So here we  have the composer command used to create a new Moodle project using composer.
### Chapitre 4 : Demo

 I'm currently using  a custom package server. So that's because we  don't want to pollute the packages ecosystem until  these packages are complete and we're happy with the suggested approach. This demonstrates the  installation of the Moodle seed package which defines the standard dependencies for a new  installation and we're generating that into the composer demo location. You can see that we are installing the Moodle composer installer package which is a composer plugin, the Moodle  Moodle source code and the Moodle Moodle composer scaffold package as well as some dependencies for  that scaffolding. 

### Chapitre 5 : Scaffolding

You'll notice that after the  composer installation is complete, it asks if we  would like to create a Moodle configuration file.  We can follow through these questions accepting  the defaults to create a basic config.php  config.php file. And we're then invited to  install Moodle 2. So I'm going to follow  through that. Now this is the Moodle scaffolder at work. Essentially because the Moodle directory is  now under the control of composer, we cannot keep  data such as a config.php file in that location  as it will get removed any time that Moodle is  updated. So to solve this problem, we generate  a shim configuration file which loads the real  configuration from our composer demo directory.  You'll notice that I wasn't actually asked that  many questions there. And that's because I have  a N file in my current location which  contains my default configuration. And that's  great for people like myself who create lots of  Moodle sites.

### Chapitre 6 : The root package

 So if we change directory into that  composer demo location, you see that we have what  is known as our root package. Okay. And we have a  only we only have a few files in there. We have  the git ignore file which we've just generate or copied from our seed project. And that's for  people who may want to manage this installation  using git. It's optional. You can remove it.  There's no uses. This isn't currently git tracked.  We also have the composer.json file which is the  default value copied from our C project. And the  composer.ck file generated during that composer  install. We have the config.php file that was just generated. a data gen directory which was  also generated during that scaffolding process. The Moodle folder contains the Moodle source  code that we all know and love. And we have our vendor directory which contains the other composer  dependencies.  

### Chapitre 7 : Configuration and Shims

Now you may notice that we have two copy.php files. One is in our root package and  the other one is in our Moodle directory. And  if we have a look at that Moodle config.php file,  you'll see that shim file that I mentioned before.  Now this is automatically generated any time we  perform any composer install or composer update  for any composer package and this is the loading  the real config.php from our root package. It's  also lo also loading the vendor autoload.php  and the moodle setup file. The real config.php  is as I say in our root package and that contains  the values which we've generated as part of that setup process. 

### Chapitre 8 : About my setup

Now I'll stop here and  I will mention that none of this content is web  accessible. The way that I have configured my  development environment is that each site has a directory in sites Moodle in my home directory  and each of those contains all of its source and  its data. So I have in here as you can see the  Moodle directory and the data directory and that's   typical for all of the Moodle installations that  I have for the last few years. The site itself is  actually served from a different location. my document route is in site's public html and each of those is a sim link. Each of those locations  is a sim link to the relevant location. So in this case is a sim link to the moodle/public directory  and that's typical for all of my Moodle sites.  
### Chapitre 9 : Installing a Moodle Plugin

Excellent. Now that our Moodle installation has  finished, I can also add a plug-in dependency.  Now, before we start, I'll show you that my  Moodle public mod folder just contains the standard Moodle plugins. Importantly, I haven't  got custom in there, which is the plug-in that  I'm going to install. Now, I'm using a custom  version of customert, which I have put onto my packagist server for development. And  that's just has a custom composer.json file  with a release so that I can do this testing.  You'll see that that installed the version 5.2,2, which is a fake version of customert. And they  didn't install it into the vendor directory. No, it actually installed it into the Moodle public mod folder. And we now have a  customert directory there. Inside there, we  also have our composer.json file. And that's  how this magic happens. This composer.json  file defines two things. Firstly is a type and that is set to moodle  the plug-in  type. Now this plug-in type is moodle composer.Let's get rid of that. Um that's what mood-moded.  That's the Moodle uh mod is the plug-in type for  this. We also have a dependency on the Moodle  composer installer. And it that uh plugin that  composer plugin takes the moodle um plug-in type  here and it uses a map to put those into the correct location. We also me specify the version  of Moodle that we want to use. Now this is version compatible with version 4.5 and on. And so we  say that's the case and composer can then use  that information to make sure that the correct  version of the plugin is installed for the  correct version of Moodle. 
### Chapitre 10 : Metapackages and development

 It's also to possible  to create composer meta packages to load a set  or a collection of Moodle plugins as a or to load  other features like open telemetry. And I'm just  going to switch to a development branch of Moodle  here using the composer repository command and requiring a dev version of Moodle. Now on its  own that doesn't actually do everything. Also,  while I'm doing that, I'm going to upgrade  Moodle and run that in the background. So, we've  um we've switched to a a development version of  Moodle. And now that points to a is a sim link to  my development git branch uh git repository with  a particular branch checked out which contains my  open telemetry code for example. Now I also need  to install some open telemetry extensions. Now  because we can't put these into the Moodle  composer.json because they would always be  installed and uh open telemetry also requires a  custom u PHP extension is compiled and installed  which isn't very friendly. Uh we've put that  into a composer package which you can include   in your root package. So by doing that and it's  going to ask me a couple of questions as well.
and reinstalling all of these open telemetry  plugins including the SDK. And once that's done, I should be able to pop over to my composer demo  site. This is the first time I'm loading it. And  I can log in here with my extreme extremely secure  password of test which Chrome will compose about  complain about. And immediately my traces should  show up and I can have a look at my root spans  and see that all of these things are now showing  up. Uh, and that's all the configuration we've had   to do within Moodle. And that's the end of the  demo. We have a fully functional Moodle site.   There's still a bit of configuration to do here,  but everything is there and working. Thank you.